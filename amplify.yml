version: 1
applications:
    -
        frontend:
            phases:
                build:
                    commands: ['yarn build']
            artifacts:
                baseDirectory: build
        phases:
            preBuild:
                commands:
                    - 'yarn install'
            build:
                commands:
                    - 'yarn build'
        artifacts:
            baseDirectory: build
            files:
                - '**/*'
        cache:
            paths:
                - 'node_modules/**/*'
        appRoot: frappe-gantt
    -
        frontend:
          phases:
              preBuild:
                  commands:
                      - 'yarn install'
              build:
                  commands:
                      - 'yarn build'
          artifacts:
              baseDirectory: build
              files:
                  - '**/*'
          cache:
              paths:
                  - 'node_modules/**/*'
          appRoot: orgchart
